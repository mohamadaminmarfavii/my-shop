# OMS Pro — گزارش تحلیل پیش از بازنویسی

این گزارش نتیجه بررسی مستقیم دو فایل ضمیمه‌شده است:
`oms-pro-v9.4.7-report-profit-integrity-fix.html` (حدود ۱.۶ مگابایت) و پشتیبان کامل JSON (حدود ۵.۴ مگابایت، شامل ۳٬۰۵۶ محصول و ۷۶ سفارش واقعی).

---

## ۱. معماری فعلی برنامه

فایل HTML یک باندل Vite/React (React 19، minified) است که یک اپ React واقعی را در `#root` رندر می‌کند. اما داخل همین باندل، یک ثابت به‌نام
`LEGACY_HTML` وجود دارد که **کل یک نسخهٔ قدیمی‌تر برنامه (v9.1.6) را به‌صورت یک رشتهٔ HTML کامل** (با `<head>`، استایل کامل Tailwind، و باندل React خودش) در خود جای داده — یعنی دقیقاً همان ساختار "wrapper روی نسخهٔ قبلی" که شما توصیف کردید. این رشته احتمالاً در یک `iframe` یا innerHTML رندر می‌شود، یعنی در واقع **دو کپی کامل از React + Tailwind + کل منطق برنامه** هم‌زمان در حافظهٔ مرورگر هستند.

نکات دیگر معماری فعلی:
- کتابخانهٔ کامل **SheetJS (xlsx.js)** — که خودش صدها کیلوبایت است — داخل همان باندل اصلی import شده، حتی اگر هر صفحه به آن نیاز نداشته باشد.
- یک لایهٔ همگام‌سازی با **Supabase** (Postgres آنلاین) هم در تنظیمات وجود دارد که کل payload برنامه را به‌صورت یک ستون `jsonb` واحد در جدول `oms_data` ذخیره/بازیابی می‌کند — یعنی حتی لایهٔ "آنلاین" هم هیچ ساختار رابطه‌ای ندارد و کل داده یک blob است.
- ذخیرهٔ محلی از یک IndexedDB ساده (`oms_pro_mobile_db`, فقط دو object store به‌نام `kv` و `backups`) با یک کلید تکی (`main`) استفاده می‌کند — یعنی IndexedDB هم فقط به‌عنوان ظرف یک JSON بزرگ به کار رفته، نه به‌صورت جدول‌های ایندکس‌شده.

## ۲. علت احتمالی کندی

- **رندر تکراری**: وجود دو کپی کامل از اپ (نسخهٔ جدید + `LEGACY_HTML` داخل آن) یعنی دو برابر کد JS پارس/اجرا می‌شود و دو برابر DOM ساخته می‌شود، حتی اگر فقط یکی دیده شود.
- **بدون تفکیک ماژول/لایو‌لود**: همه‌چیز (از جمله SheetJS کامل) در یک باندل واحد است؛ ورود به هر تب باعث اجرای کل این حجم کد می‌شود، نه فقط بخش مربوطه.
- **بدون ایندکس واقعی**: چون IndexedDB فقط یک blob تکی نگه می‌دارد، هر خواندن/نوشتن یعنی سریالایز/دسریالایز کل دیتابیس (۳٬۰۵۶ محصول + ۷۶ سفارش + ...، که با رشد داده فقط بدتر می‌شود)، نه یک تراکنش کوچک روی جدول مربوطه.
- محاسبات مشتق (سود، جمع‌ها) ظاهراً در چند نقطهٔ مختلف کد (کامپوننت داشبورد، صفحهٔ گزارش) به‌صورت جدا از هم بازمحاسبه می‌شوند به‌جای یک selector مشترک و memoize‌شده — این هم روی سرعت اثر می‌گذارد، هم روی صحت اعداد (بخش ۳).

## ۳. محل دقیق چندمنبعی‌شدن محاسبات

نکتهٔ مهم و امیدوارکننده: در همین نسخهٔ v9.4.7 یک تابع (`OMS_orderComputedMetrics`) اضافه شده که تلاش می‌کند سود را از روی اقلام سفارش بازمحاسبه کند و اگر عدد ذخیره‌شدهٔ `order.profit` "غیرمنطقی" باشد (خارج از یک بازهٔ محافظه‌کارانه) آن را نادیده بگیرد. این دقیقاً همان patch نسبت به باگ #۷ (سود میلیاردی) است — اما مشکل این‌جاست که:

- این تابع فقط در یک کامپوننت (به نظر می‌رسد داشبورد) صدا زده می‌شود، نه در همهٔ صفحات (گزارش روزانه/ماهانه، حساب تأمین‌کننده، سود شخصی، سهم همکاری). یعنی همان صفحه‌ای که این patch را دارد عدد درست نشان می‌دهد و بقیهٔ صفحات همچنان می‌توانند به `order.profit` خام اعتماد کنند.
- این خودش نمونهٔ زندهٔ همان الگویی است که شما نگرانش هستید: **یک محاسبه، به‌جای این‌که در یک منبع مشترک باشد، به‌صورت جداگانه در هر کامپوننتی که به آن نیاز دارد کپی/patch می‌شود.**

با تحلیل ۷۶ سفارش پشتیبان: در **۱۵ سفارش (حدود ۲۰٪)** مقدار `profit` ذخیره‌شده با بازمحاسبهٔ ساده از روی `revenue - Σ(buyPrice) - snappFee` مطابقت ندارد — یعنی حتی همین یک فرمول ساده هم به‌صورت پایدار در همهٔ سفارش‌ها اعمال نشده (احتمالاً به‌خاطر تخفیف/برگشت‌وجه دستی که در برخی سفارش‌ها لحاظ شده و در برخی نه).

### یافتهٔ مهم دربارهٔ باگ #۱ (پرداخت تسویه نمی‌کند)
در کل پشتیبان JSON **هیچ رکورد SupplierPayment یا PaymentAllocation‌ای وجود ندارد** — نه صفر رکورد به‌خاطر نبود پرداخت، بلکه اصلاً چنین جدولی در مدل دادهٔ فعلی تعریف نشده. تنها چیزی که هست یک فیلد `settlementStatus` (`"تسویه شده"` / `"تسویه نشده"`) روی خودِ سفارش است — یعنی **وضعیت تسویه یک flag دستی/ذخیره‌شده روی سفارش است، نه چیزی که از روی پرداخت‌های واقعی مشتق شود.** این دقیقاً ریشهٔ باگ #۱ است: وقتی پرداختی ثبت می‌شود، هیچ مکانیزم ساختاریافته‌ای برای اتصال آن به اقلام سفارش و به‌روزرسانی مانده وجود ندارد، چون خودِ مفهوم "تخصیص پرداخت به قلم" هرگز در مدل داده تعریف نشده بوده.

**نتیجه برای مهاجرت**: تاریخچهٔ پرداخت‌های تأمین‌کننده و تخصیص آن‌ها به اقلام، در پشتیبان فعلی قابل بازسازی خودکار نیست (چون هرگز ذخیره نشده). Migration Wizard فقط می‌تواند وضعیت *فعلی* (`settlementStatus` هر سفارش) را به‌عنوان یک نقطهٔ شروع علامت بزند، ولی مبلغ دقیق/تاریخ هر پرداخت گذشته باید توسط شما به‌صورت دستی وارد یا تأیید شود. این یکی از مهم‌ترین آیتم‌های «نیازمند تصمیم شما» است (بخش پایانی).

## ۴. ساختار دادهٔ فعلی (از روی پشتیبان واقعی)

```
{
  products: Product[3056],       // شامل هم محصولات خودتان و هم AliProducts sync‌شده
  orders: Order[76],             // فیلدهای items[], profit, revenue, snappFee, snappCommission,
                                  // snappVat, settlementStatus, supplier, customerCode, ...
  aliProducts: Product[2882],    // کش محصولات سایت علی ورساچ
  aliLastSync: number,
  sites: Site[1],                // شامل یک Application Password وردپرس به‌صورت متن ساده
  settings: { ...supabaseUrl, supabaseKey, ... },
  notifications: Notification[80]
}
```

نکات:
- هیچ‌کدام از موجودیت‌های مالی مستقل که در طرح شما آمده (`SupplierPayment`, `PaymentAllocation`, `CustomerReceipt`, `SnappSettlement`, `Expense`, `CustomerRefund`, `SupplierRefund`, `ProfitShareRule/Payment`, `CashWithdrawal`, `OpeningBalance`, `AuditLog`) در داده فعلی وجود ندارند — همه باید از صفر ساخته شوند.
- اسنپ‌پی فیلدهایش مستقیم روی سفارش است (`snappFee`, `snappCommission`, `snappVat`) نه یک رکورد جدا؛ نگهداری ۱۰٪ و آزادسازی آن هیچ نمایندگی در داده ندارد.
- **نکتهٔ امنیتی**: فایل `sites[0].appPassword` یک Application Password واقعی وردپرس را به‌صورت متن ساده در خود دارد. توصیه می‌کنم این رمز را هرچه زودتر در وردپرس *rotate* کنید و در نسخهٔ جدید هرگز چنین رمزی را در دادهٔ قابل export/پشتیبان ذخیره نکنید (به‌جای آن یک secret جدا و رمزنگاری‌شده، یا حتی بهتر، خارج از IndexedDB).

## ۵. خطرهای مهاجرت

| خطر | توضیح |
|---|---|
| بازسازی نشدنی بودن تاریخچهٔ پرداخت | چون `SupplierPayment` هرگز وجود نداشته، تاریخچهٔ دقیق پرداخت‌ها فقط از حافظهٔ شما قابل بازسازی است، نه از داده |
| نام‌های متغیر تأمین‌کننده | در دادهٔ فعلی فقط `"علی ورساچ"` و `"وحید نظری"` دیده شد، ولی طبق گفتهٔ شما نسخه‌های `علی ورساچه`/`Ali Versace`/`وحید` هم ممکن است جای دیگری (مثلاً یادداشت‌ها) باشند؛ باید در Migration Wizard فهرست کامل نام‌های خام قبل از یکسان‌سازی نمایش داده شود، نه فقط یکسان‌سازی خودکار |
| `profit` نامعتبر در ۲۰٪ سفارش‌ها | باید علامت‌گذاری و در گزارش پیش‌نمایش مهاجرت نشان داده شود، نه سکوت یا حذف خودکار |
| مرز مرداد/شهریور | `orderDateFa` به‌صورت رشتهٔ فارسی (`"۱۴۰۵/۶/۱۳"`) ذخیره شده، نه یک نوع تاریخ قابل مرتب‌سازی مستقیم؛ باید حتماً قبل از مهاجرت به یک کلید دورهٔ استاندارد (`periodKey`, مثلاً `"1405-06"`) تبدیل و اعتبارسنجی شود |
| Application Password در پشتیبان | باید قبل از تحویل هر خروجی/گزارش از این پروژه به جای دیگر (فایل، ایمیل و غیره)، از داده حذف یا جایگزین شود |

## ۶. برنامهٔ مرحله‌به‌مرحله بازنویسی

1. **مدل مالی واحد** (تمام‌شده در این مرحله) — تایپ‌های TypeScript برای همهٔ ۱۴ موجودیت + موتور محاسباتی خالص (`src/finance/engine.ts`) که سود تعهدی، سود وصول‌شده، سود قابل‌برداشت، تخصیص پرداخت idempotent، و فرمول‌های قیمت‌گذاری علی/وحید را پیاده می‌کند.
2. **تست‌های خودکار روی همان موتور** (تمام‌شده) — ۲۱ تست Vitest که مستقیماً سناریوهای باگ ۱ تا ۷ و چند سناریوی دیگر از فهرست ۲۵‌تایی شما را پوشش می‌دهند (فهرست کامل در بخش ۷).
3. لایهٔ **IndexedDB با Dexie** (اسکلت تمام‌شده در `src/db/schema.ts`) — جدول‌های جدا برای هر موجودیت؛ `PaymentAllocation` به‌عمد یک view مشتق است، نه جدول قابل ویرایش دستی.
4. رابط کاربری React برای هر صفحه (داشبورد، ثبت پرداخت، حساب تأمین‌کنندگان، گزارش‌ها، ...) — همه باید فقط از توابع مرحلهٔ ۱ بخوانند.
5. **Migration Wizard** — Dry Run، تشخیص ناسازگاری‌ها (جدول بخش ۵)، پشتیبان کامل قبل از هر تغییر، اجرای واقعی فقط با تأیید صریح شما.
6. تبدیل به **PWA** واقعی (manifest، Service Worker، آفلاین، دکمهٔ آپدیت) — آخرین مرحله، بعد از ثبات منطق.
7. Build نهایی برای GitHub Pages + مستندسازی (README اجرا/build/آپلود + چک‌لیست تست دستی فارسی).

## ۷. تست‌هایی که قبل از تغییر منطق نوشته و اجرا شدند

هر ۲۱ تست زیر با `npx vitest run` روی موتور جدید **سبز** هستند (فایل: `src/finance/__tests__/engine.test.ts`):

- پرداخت هدفمند ۱٬۶۵۰٬۰۰۰ وحید → هر دو قلم خانم شعبانی دقیقاً صفر می‌شوند (باگ #۱)
- همان پرداخت هیچ اثری روی حساب علی ندارد (باگ #۲ ایزوله‌سازی)
- ماندگاری `targetMode`/`targetItemIds` در یک round-trip سریالایز (معادل رفرش صفحه)
- اجرای دوبارهٔ `recomputeSupplierAllocations` نتیجهٔ یکسان می‌دهد (idempotent بودن سینک)
- حذف نرم یک پرداخت → اثر آن دقیقاً یک‌بار حذف می‌شود؛ بازیابی → دقیقاً یک‌بار برمی‌گردد و با حالت اولیه برابر است
- پرداخت بیشتر از بدهی باز → هرگز مانده منفی نمی‌سازد؛ مازاد فقط تخصیص‌نیافته می‌ماند
- سفارش لغوشده در فروش/سود صفر است؛ سود همیشه از روی اقلام بازمحاسبه می‌شود، نه از فیلد ذخیره‌شده
- پارس اعداد فارسی/عربی/انگلیسی و جداکننده‌های «٬»/«,»؛ رد کردن ورودی نامعتبر به‌جای صفر خاموش
- فرمول قیمت‌گذاری علی دقیقاً با دو مثال عددی شما (۵۹۵هزار و ۲.۱میلیون) تطبیق دارد
- پیشنهاد آخرین قیمت فروش برای محصولات وحید (نه فرمول درصدی علی)
- سهم همکاری ۵۰٪ هیچ اثری روی بدهی خرید نمی‌گذارد
- سود قابل‌برداشت با صندوق خالی صفر می‌شود، حتی اگر سود تعهدی مثبت باشد؛ و با تعهدات فوری محدود می‌شود
- واریزی اسنپ‌پی فقط بخش تأییدشدهٔ ۹۰٪ را «وصول‌شده» حساب می‌کند، نه کل مبلغ ثبت‌شده

## ۸. وضعیت فعلی پروژه (صادقانه، نه «حل‌شده»)

طبق قانونی که خودتان گذاشتید، این کار را «حل‌شده» اعلام نمی‌کنم — چیزی که تا این لحظه واقعاً ساخته و تست شده:

- اسکلت پروژهٔ Vite + React + TypeScript (`npm run build` و `npx tsc --noEmit` هر دو بدون خطا)
- مدل داده کامل (۱۴ موجودیت) به‌صورت type
- موتور محاسباتی مالی با ۲۱ تست سبز (۲۱ تست موتور)
- تست‌های نگاشت مهاجرت و بررسی‌های پیش از مهاجرت (Dry Run) — روی هر ۷۶ سفارش واقعی شما، صفر خطای مسدودکننده
- تست‌های اعتبارسنجی فایل پشتیبان (schema version، ساختار جدول‌ها)
- تست‌های تبدیل تاریخ میلادی→شمسی، انکر‌شده به تاریخ واقعی یکی از سفارش‌های شما
- تست‌های گروه‌بندی گزارش‌ها و تفکیک سود شخصی/سهم شریک
- **مجموعاً ۵۳ تست خودکار سبز** (`npm test`)، `npm run build` و هر دو `typecheck` بدون خطا
- لایهٔ Dexie/IndexedDB با جدول‌های جدا برای هر موجودیت (`src/db/schema.ts`)
- لایهٔ دسترسی به داده با AuditLog خودکار روی هر نوشتن (`src/db/repo.ts`)
- تابع نگاشت دادهٔ قدیمی به مدل جدید + آزمایش‌شده روی **هر ۷۶ سفارش واقعی شما**، بدون کرش (بخش ۱۰)
- ۸ صفحهٔ کارکردی React با مسیریابی hash-based (سازگار با GitHub Pages):
  - **داشبورد**: فروش/بهای خرید/سود تعهدی/سود وصول‌شده/موجودی صندوق/سود قابل‌برداشت، همه به‌صورت زنده از موتور محاسباتی
  - **حساب تأمین‌کنندگان**: جدول اقلام باز + فرم ثبت پرداخت (FIFO یا هدفمند) + حذف نرم/بازیابی پرداخت + دکمهٔ همگام‌سازی امن
  - **اقلام سفارش‌ها**: همان اعداد پرداخت/مانده، از همان منبع محاسباتی حساب تأمین‌کنندگان
  - **گزارش روزانه** و **گزارش ماهانه**: همان ردیف محاسباتی داشبورد، گروه‌بندی‌شده بر اساس تاریخ شمسی واقعی (تبدیل میلادی→شمسی با آفست ثابت ایران، تست‌شده روی تاریخ واقعی سفارش‌های شما)
  - **سود شخصی**: تفکیک سود وصول‌شده به سهم شخصی/سهم شریک بر اساس قوانین سهم همکاری، بدون هیچ اثری روی بدهی خرید تأمین‌کننده
  - **مهاجرت از پشتیبان قدیمی**: Dry Run واقعی روی فایل شما → گزارش کامل خطا/هشدار (شامل تشخیص سود ذخیره‌شدهٔ غیرمنطقی) → اگر خطای مسدودکننده نبود، دکمهٔ «تأیید و اجرا» فعال می‌شود → قبل از هر نوشتنی یک پشتیبان کامل خودکار دانلود می‌شود → بعد سفارش‌ها/اقلام وارد می‌شوند
  - **پشتیبان‌گیری و بازیابی**: دانلود پشتیبان کامل JSON (با نسخهٔ schema) در هر لحظه؛ بازیابی فقط بعد از اعتبارسنجی فایل و نمایش جدول «تعداد فعلی در برابر تعداد در پشتیبان»
- **PWA واقعی**: manifest + Service Worker (با `vite-plugin-pwa`)، آیکن‌های ۱۹۲/۵۱۲/maskable، نصب‌شدنی روی Android و Windows از آدرس HTTPS، اجرای آفلاین (چون دادهٔ مالی همیشه در IndexedDB است، نه در Cache Storage)، دکمهٔ «به‌روزرسانی برنامه» با کنترل دستی کاربر، نمایش شمارهٔ نسخه در پایین صفحه
- **base path قابل‌تنظیم** برای GitHub Pages (`VITE_BASE_PATH=/repo-name/ npm run build`) — تست‌شده که مسیرهای HTML/manifest درست تولید می‌شوند
- **README کامل** با دستور دقیق اجرا/تست/build/آپلود روی GitHub Pages + چک‌لیست تست دستی فارسی

چیزی که هنوز ساخته **نشده** (مراحل بعدی):
- صفحهٔ مشتریان (فهرست/تاریخچهٔ خرید مشتری)
- رابط کاربری تنظیمات قیمت‌گذاری (فرمول‌های علی/وحید در موتور محاسباتی و تست‌ها آماده‌اند، ولی صفحهٔ تنظیمات برای وارد کردن درصدها هنوز ساخته نشده)
- ویرایش یک پرداخت ثبت‌شده (فعلاً فقط ایجاد/حذف/بازیابی)
- نوع پرداخت جداگانه برای «پرداخت مستقیم مشتری به تأمین‌کننده»
- وارد کردن واقعی و دائمی دادهٔ شما (فعلاً باید از تب «مهاجرت از پشتیبان قدیمی» با تأیید خودتان انجام شود؛ تا این لحظه در پروژهٔ نمونه اجرا نشده)
- آیکن‌های نهایی برندشدهٔ PWA (آیکن فعلی یک placeholder ساده با حروف OMS است)
- تست دستی واقعی نصب PWA روی یک گوشی/ویندوز واقعی از آدرس GitHub Pages (تا این لحظه فقط build محلی تأیید شده، نه نصب واقعی)

## ۱۰. نتیجهٔ آزمایش نگاشت مهاجرت روی ۷۶ سفارش واقعی

تابع `mapLegacyOrder` روی هر ۷۶ سفارش پشتیبان واقعی اجرا شد (بدون Dexie، فقط منطق خالص):

- **صفر کرش** — همهٔ ۷۶ سفارش بدون خطا map شدند.
- **۳۳ هشدار**، دقیقاً به این تفکیک:
  - ۱۷ سفارش: نام تأمین‌کننده خالی/نامشخص
  - ۳ سفارش: نام تأمین‌کننده رشتهٔ خالی (`""`)
  - ۱۳ سفارش: کانال فروش (`orderChannel`) ثبت نشده
- همهٔ مقادیر `status` موجود (`"تحویل شد"`, `"درحال تامین"`) شناخته‌شده بودند — هشداری برای وضعیت ناشناخته نیامد.

این ۲۰ سفارش با تأمین‌کنندهٔ نامشخص و ۱۳ سفارش با کانال نامشخص همان‌هایی هستند که Migration Wizard نهایی باید قبل از هر مهاجرت واقعی، تک‌به‌تک برای تأیید شما نمایش دهد.

همچنین بررسی پیش از مهاجرت (`runPreflightChecks`) اکنون به‌صورت خودکار همان **۱۵ سفارشی که سود ذخیره‌شدهٔ غیرمنطقی دارند** (بخش ۳) را هم به‌عنوان هشدار «سود ذخیره‌شده غیرممکن» شناسایی و در گزارش Dry Run فهرست می‌کند — این دیگر یک یافتهٔ دستی نیست، بلکه بخشی از خود Migration Wizard است و هر بار که فایل جدیدی بدهید دوباره اجرا می‌شود.

## ۹. فهرست موارد نیازمند تصمیم شما

1. **فرمول دقیق گردکردن روان‌شناختی قیمت** — مثال‌های شما با علامت «حدود» بودند و به دو الگوی ریاضی متفاوت اشاره می‌کردند؛ لطفاً قانون دقیق (مثلاً «همیشه ختم به ۹۰۰,۰۰۰» یا «ختم به ۹۰۰» بسته به رقم) را مشخص کنید.
2. **تاریخچهٔ پرداخت‌های تأمین‌کننده** — چون در داده فعلی ذخیره نشده، باید مشخص کنید که مانده‌های باز فعلی هر تأمین‌کننده را با چه مرجعی (حافظه، دفترچه، تلگرام و ...) در Migration Wizard تأیید می‌کنید.
3. **رمز عبور وردپرس در پشتیبان** — تأیید کنید که آن را rotate می‌کنید و نسخهٔ جدید کجا/چگونه ذخیره‌اش کند (پیشنهاد: اصلاً داخل IndexedDB/پشتیبان نه).
4. **منبع `periodKey`** — آیا مبنای تفکیک ماه همیشه اول ماه شمسی است، یا گاهی دوره‌های حسابداری متفاوت (مثلاً چرخهٔ نگهداری اسنپ‌پی) دارید؟
