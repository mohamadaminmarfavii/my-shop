import {summary,supplierRows,orderAmounts} from '../domain/finance.js';import {head,button,money,e,notice,table,amountColumn,explain,bind,supplier} from '../ui.js';
export function render(root,ctx){const s=ctx.state,m=ctx.metrics();root.innerHTML=head('نمای کلی حساب‌ها',`حساب جاری از ${s.meta.start} · همه مبلغ‌ها به تومان`,button('ثبت پرداخت','pay','primary')+button('ثبت سفارش','order'))+(m.unknown?notice('اطلاعات مالی کامل و تأییدشده نیست. جمع‌های موجود موقت‌اند و سود قابل برداشت مسدود است.'):'')+(!s.orders.length?`<div class="welcome"><div><h2>دفتر جدیدت آماده است</h2><p>برای انتقال سفارش‌ها، ابتدا پشتیبان قبلی را بررسی کن؛ قبل از تأیید، چیزی جایگزین نمی‌شود.</p><a class="button primary" href="#backup">بررسی پشتیبان قبلی</a></div><div class="welcome-symbol">OMS<span>دفتر سفارش و حساب</span></div></div>`:'')+`<div class="metrics">${[['فروش خالص',m.sales,'sales'],['سود نهایی',m.profit,'profit'],['موجودی صندوق',m.cash,'cash'],['سود قابل برداشت',m.withdrawable,'withdrawable']].map(([label,value,key],i)=>`<article class="metric ${i===3?'accent':''}"><span>${label}${m.unknown?' · موقت':''}</span><strong>${money(value)}</strong><small>تومان</small>${button('نحوه محاسبه',key,'link')}</article>`).join('')}</div><div class="two-col"><section class="panel"><div class="section-head"><h2>حساب تأمین‌کنندگان</h2><a href="#suppliers">مشاهده اقلام ←</a></div>${supplierRows(s).map(r=>`<div class="supplier-line"><span class="avatar">${e(r.name.slice(0,1))}</span><div><strong>${e(r.name)}</strong><small>${r.balance<0?'اعتبار نزد تأمین‌کننده':'مانده خرید کالا'}</small></div><b>${money(r.balance)}</b></div>`).join('')}</section><section class="panel"><h2>وصول و تعهدات</h2><dl>${[['سود وصول‌شده',m.realized],['سهم همکاری وصول‌شده',m.partnerRealized],['اسنپ‌پی در انتظار',m.snappPending],['نگهداری ۱۰٪ دریافت‌نشده',m.retention]].map(([k,v])=>`<div><dt>${k}</dt><dd>${money(v)}</dd></div>`).join('')}</dl><a href="#ledger">ثبت دریافت و واریزی ←</a></section></div><section class="panel"><div class="section-head"><h2>سفارش‌های اخیر</h2><a href="#orders">همه سفارش‌ها ←</a></div><div id="recent"></div></section>`;
 table(root.querySelector('#recent'),[{key:'date',label:'تاریخ'},{key:'customer',label:'مشتری'},{key:'code',label:'کد سفارش'},amountColumn('sales','فروش خالص'),{key:'statusLabel',label:'وضعیت'}],s.orders.filter(o=>o.date>=s.meta.start).sort((a,b)=>b.date.localeCompare(a.date)).slice(0,8).map(o=>({...o,sales:orderAmounts(s,o).netSales,statusLabel:o.needsReview?'نیازمند بررسی':o.status==='cancelled'?'لغوشده':'فعال'})),{search:false});bind(root,{pay:()=>ctx.payment(),order:()=>ctx.order(),cash:()=>explain('محاسبه صندوق',m.explanation),withdrawable:()=>explain('حداقل سود باقی‌مانده و پول آزاد صندوق',m.explanation),sales:()=>location.hash='reports',profit:()=>location.hash='reports'});
}
